The goal of this project is to download a dataset that is not pre-cleaned, meaning it will include a lot of data that might not be relevant, features that include NaN's and so on.

Learning goal:
- Transform a huge dataset into a smaller dataset with relevant features
